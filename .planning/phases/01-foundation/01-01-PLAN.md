---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - animecix-v2/package.json
  - animecix-v2/tsconfig.json
  - animecix-v2/forge.config.ts
  - animecix-v2/vite.main.config.mjs
  - animecix-v2/vite.preload.config.mjs
  - animecix-v2/vite.renderer.config.mjs
  - animecix-v2/vitest.config.ts
  - animecix-v2/src/types/animecix-api.d.ts
  - animecix-v2/src/storage/StorageService.ts
  - animecix-v2/src/storage/schema.ts
  - animecix-v2/tests/storage/StorageService.test.ts
  - animecix-v2/tests/security/webPreferences.test.ts
  - animecix-v2/tests/security/certValidation.test.ts
  - animecix-v2/tests/preload/animecixAPI.test.ts
  - animecix-v2/tests/window/WindowService.test.ts
  - animecix-v2/tests/main/singleInstance.test.ts
autonomous: true
requirements:
  - NET-02
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Electron Forge project scaffolded with Vite+TypeScript and builds successfully"
    - "better-sqlite3 rebuilds for Electron ABI without errors"
    - "AnimecixAPI type contract defines all Phase 1 IPC methods"
    - "StorageService creates SQLite database and performs CRUD on settings and window_bounds"
    - "Vitest test infrastructure is installed and test stubs exist for all Phase 1 requirements"
  artifacts:
    - path: "animecix-v2/forge.config.ts"
      provides: "Forge config with AutoUnpackNatives, Vite plugin, Fuses, rebuildConfig"
      contains: "AutoUnpackNativesPlugin"
    - path: "animecix-v2/vite.main.config.mjs"
      provides: "Main process Vite config with better-sqlite3 external"
      contains: "better-sqlite3"
    - path: "animecix-v2/src/types/animecix-api.d.ts"
      provides: "AnimecixAPI interface + Window augmentation"
      contains: "interface AnimecixAPI"
    - path: "animecix-v2/src/storage/StorageService.ts"
      provides: "SQLite storage wrapper for settings and window bounds"
      exports: ["StorageService"]
    - path: "animecix-v2/src/storage/schema.ts"
      provides: "CREATE TABLE definitions for settings and window_bounds"
      contains: "CREATE TABLE"
    - path: "animecix-v2/vitest.config.ts"
      provides: "Vitest configuration for Electron project testing"
      contains: "defineConfig"
  key_links:
    - from: "animecix-v2/forge.config.ts"
      to: "animecix-v2/vite.main.config.mjs"
      via: "VitePlugin build entries reference config files"
      pattern: "vite\\.main\\.config"
    - from: "animecix-v2/src/storage/StorageService.ts"
      to: "animecix-v2/src/storage/schema.ts"
      via: "imports schema SQL strings"
      pattern: "import.*schema"
---

<objective>
Scaffold the Electron Forge project with Vite+TypeScript, install and configure all dependencies (including better-sqlite3 native module rebuild), set up vitest test infrastructure with stubs for all Phase 1 requirements, define the typed AnimecixAPI contract, and implement StorageService.

Purpose: Every subsequent task in Phase 1 (and beyond) builds on this scaffold. The type contract prevents API drift between preload and consumers. StorageService is needed by Plan 02 for window bounds persistence. Test stubs enable automated verification for all subsequent tasks.

Output: A buildable Electron Forge project in animecix-v2/ with typed IPC contracts, working SQLite storage, and vitest test infrastructure.
</objective>

<execution_context>
@/Users/emirhaneyidogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emirhaneyidogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Electron Forge project and configure build tooling</name>
  <files>
    animecix-v2/package.json,
    animecix-v2/tsconfig.json,
    animecix-v2/forge.config.ts,
    animecix-v2/vite.main.config.mjs,
    animecix-v2/vite.preload.config.mjs,
    animecix-v2/vite.renderer.config.mjs
  </files>
  <action>
1. Run `npx create-electron-app@latest animecix-v2 --template=vite-typescript` from the project root (/Users/emirhaneyidogan/Projeler/AnimeciX Desktop Apps/).

2. Inside animecix-v2/, install dependencies:
   ```
   npm install better-sqlite3
   npm install --save-dev @types/better-sqlite3 @electron-forge/plugin-auto-unpack-natives electron-rebuild vitest @vitest/coverage-v8
   ```

3. Add postinstall script to package.json: `"postinstall": "electron-rebuild -f -w better-sqlite3"`

4. Run `npm run postinstall` to verify better-sqlite3 rebuilds for Electron ABI. If it fails, replace better-sqlite3 with node-sqlite3-wasm (same API surface) and skip the rebuild config.

5. Update forge.config.ts to match the research template exactly:
   - Add AutoUnpackNativesPlugin
   - Add rebuildConfig with `force: true, buildFromSource: true, onlyModules: ['better-sqlite3']`
   - Add FusesPlugin with RunAsNode:false, EnableCookieEncryption:true, EnableNodeOptionsEnvironmentVariable:false
   - Keep VitePlugin with build entries for src/main.ts and src/preload.ts

6. Update vite.main.config.mjs: add `build.rollupOptions.external: ['better-sqlite3']`

7. Update vite.preload.config.mjs: ensure it has a clean config (no external needed since preload does not import native modules).

8. Verify the project builds: `npm run build`. Fix any build errors. The generated default main.ts and preload.ts are fine for now -- Plan 02 replaces them.

IMPORTANT security baseline (AUTH-02, AUTH-03): The scaffold's default webPreferences already set contextIsolation:true. Verify this is present in the generated code. Do NOT add nodeIntegration:true or contextIsolation:false anywhere. Do NOT add any setCertificateVerifyProc calls.
  </action>
  <verify>
    <automated>cd /Users/emirhaneyidogan/Projeler/AnimeciX\ Desktop\ Apps/animecix-v2 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - animecix-v2/ exists with Electron Forge Vite+TypeScript scaffold
    - better-sqlite3 installed and rebuilt for Electron ABI (or wasm fallback installed)
    - forge.config.ts has AutoUnpackNatives, FusesPlugin, rebuildConfig
    - vite.main.config.mjs has better-sqlite3 in external
    - `npx tsc --noEmit` succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up vitest test infrastructure and create test stubs for all Phase 1 requirements</name>
  <files>
    animecix-v2/vitest.config.ts,
    animecix-v2/tests/storage/StorageService.test.ts,
    animecix-v2/tests/window/WindowService.test.ts,
    animecix-v2/tests/main/singleInstance.test.ts,
    animecix-v2/tests/security/webPreferences.test.ts,
    animecix-v2/tests/security/certValidation.test.ts,
    animecix-v2/tests/preload/animecixAPI.test.ts
  </files>
  <action>
1. Create `vitest.config.ts` at animecix-v2/ root:
   ```typescript
   import { defineConfig } from 'vitest/config';
   export default defineConfig({
     test: {
       root: '.',
       include: ['tests/**/*.test.ts'],
       environment: 'node',
       globals: true,
     },
   });
   ```

2. Add test script to package.json: `"test": "vitest run --reporter=dot"`

3. Create test stub files with `describe` + `it.todo()` placeholders for each requirement:

   a. `tests/storage/StorageService.test.ts`:
      - describe('StorageService')
        - it.todo('creates database and initializes schema')
        - it.todo('getSetting returns null for missing key')
        - it.todo('setSetting stores and retrieves a value')
        - it.todo('getWindowBounds returns defaults on fresh DB')
        - it.todo('saveWindowBounds persists and retrieves bounds')

   b. `tests/window/WindowService.test.ts`:
      - describe('WindowService') — covers SHELL-01, SHELL-03, SHELL-04
        - it.todo('creates frameless BrowserWindow (SHELL-01)')
        - it.todo('loads animecix.tv URL (SHELL-03)')
        - it.todo('restores saved window bounds on creation')
        - it.todo('persists window bounds on resize/move')
        - it.todo('sets contextIsolation:true and nodeIntegration:false')

   c. `tests/main/singleInstance.test.ts`:
      - describe('Single Instance Lock') — covers SHELL-02
        - it.todo('calls requestSingleInstanceLock (SHELL-02)')
        - it.todo('quits if lock not obtained')
        - it.todo('focuses existing window on second-instance event')

   d. `tests/security/webPreferences.test.ts`:
      - describe('Web Preferences Security') — covers AUTH-03
        - it.todo('contextIsolation is true (AUTH-03)')
        - it.todo('nodeIntegration is false (AUTH-03)')
        - it.todo('sandbox is true (AUTH-03)')

   e. `tests/security/certValidation.test.ts`:
      - describe('Certificate Validation') — covers AUTH-02
        - it.todo('no setCertificateVerifyProc calls in codebase (AUTH-02)')
        - it.todo('no certificate bypass flags in app switches (AUTH-02)')

   f. `tests/preload/animecixAPI.test.ts`:
      - describe('AnimecixAPI Bridge') — covers NET-02
        - it.todo('exposes all AnimecixAPI methods via contextBridge (NET-02)')
        - it.todo('does not expose ipcRenderer directly')
        - it.todo('platform property matches process.platform')

4. Run `npx vitest run --reporter=dot` to verify all stubs are recognized (should show pending/todo tests, zero failures).
  </action>
  <verify>
    <automated>cd /Users/emirhaneyidogan/Projeler/AnimeciX\ Desktop\ Apps/animecix-v2 && npx vitest run --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <done>
    - vitest.config.ts exists and is valid
    - package.json has "test" script using vitest
    - All 7 test stub files exist with todo tests covering all Phase 1 requirements
    - `npx vitest run` completes without errors (all tests are todo/pending)
  </done>
</task>

<task type="auto">
  <name>Task 3: Define AnimecixAPI type contract and implement StorageService</name>
  <files>
    animecix-v2/src/types/animecix-api.d.ts,
    animecix-v2/src/storage/schema.ts,
    animecix-v2/src/storage/StorageService.ts
  </files>
  <action>
1. Create `src/types/animecix-api.d.ts` with:
   - AnimecixAPI interface defining all Phase 1 methods:
     - minimize(): Promise&lt;void&gt;
     - maximize(): Promise&lt;void&gt;
     - close(): Promise&lt;void&gt;
     - isMaximized(): Promise&lt;boolean&gt;
     - onFullscreenChange(cb: (isFullscreen: boolean) => void): () => void
     - platform: NodeJS.Platform
     - version: string | undefined
     - isOnline(): boolean
   - Global Window augmentation: `declare global { interface Window { animecix?: AnimecixAPI; } }`
   - Export the AnimecixAPI interface

   This is the contract for NET-02. The preload script (Plan 02) implements it. The website consumes it via `window.animecix`.

2. Create `src/storage/schema.ts`:
   - Export a constant `INIT_SCHEMA` containing the SQL string:
     ```sql
     CREATE TABLE IF NOT EXISTS settings (
       key   TEXT PRIMARY KEY NOT NULL,
       value TEXT NOT NULL
     );
     CREATE TABLE IF NOT EXISTS window_bounds (
       id     INTEGER PRIMARY KEY CHECK (id = 1),
       x      INTEGER,
       y      INTEGER,
       width  INTEGER NOT NULL DEFAULT 1280,
       height INTEGER NOT NULL DEFAULT 800,
       maximized INTEGER NOT NULL DEFAULT 0
     );
     INSERT OR IGNORE INTO window_bounds (id) VALUES (1);
     ```

3. Create `src/storage/StorageService.ts`:
   - Import Database from 'better-sqlite3' (or node-sqlite3-wasm if fallback was used in Task 1)
   - Import { app } from 'electron' and path from 'node:path'
   - Import { INIT_SCHEMA } from './schema'
   - Class StorageService with:
     - private db: Database.Database
     - constructor(): creates/opens DB at `path.join(app.getPath('userData'), 'animecix.db')`, runs INIT_SCHEMA
     - getSetting(key: string): string | null
     - setSetting(key: string, value: string): void
     - getWindowBounds(): { x?: number; y?: number; width: number; height: number; maximized: boolean }
     - saveWindowBounds(bounds: { x: number; y: number; width: number; height: number; maximized: boolean }): void
     - close(): void (closes DB connection gracefully)
   - Export the class

   Follow the exact implementation from 01-RESEARCH.md Pattern 4, with the addition of a close() method and the schema import.
  </action>
  <verify>
    <automated>cd /Users/emirhaneyidogan/Projeler/AnimeciX\ Desktop\ Apps/animecix-v2 && npx tsc --noEmit src/types/animecix-api.d.ts src/storage/StorageService.ts src/storage/schema.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    - AnimecixAPI interface exported with all 8 Phase 1 members
    - Window augmentation declares window.animecix as optional AnimecixAPI
    - StorageService compiles without type errors
    - schema.ts exports INIT_SCHEMA with settings and window_bounds tables
    - StorageService has getSetting, setSetting, getWindowBounds, saveWindowBounds, close methods
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` succeeds in animecix-v2/ (TypeScript compilation clean)
- `npx vitest run --reporter=dot` completes with all stubs recognized (zero failures)
- `npm run package` succeeds in animecix-v2/ (build tooling works end-to-end)
- better-sqlite3 is marked external in vite.main.config.mjs (no bundle crash)
- forge.config.ts includes AutoUnpackNatives + FusesPlugin + rebuildConfig
- AnimecixAPI.d.ts has all 8 members matching the IPC contract from CONTEXT.md
- StorageService creates tables and performs CRUD operations
</verification>

<success_criteria>
- Buildable Electron Forge project at animecix-v2/
- Vitest test infrastructure installed with test stubs for all 7 Phase 1 requirements
- Typed AnimecixAPI contract ready for preload implementation
- Working StorageService ready for window bounds persistence
- No security anti-patterns (nodeIntegration, contextIsolation bypass, cert bypass)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
