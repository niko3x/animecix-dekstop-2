import { describe, it } from 'vitest';

describe('StorageService', () => {
  it.todo('creates database and initializes schema');
  it.todo('getSetting returns null for missing key');
  it.todo('setSetting stores and retrieves a value');
  it.todo('getWindowBounds returns defaults on fresh DB');
  it.todo('saveWindowBounds persists and retrieves bounds');
});
