import { describe, it } from 'vitest';

describe('AnimecixAPI Bridge', () => {
  it.todo('exposes all AnimecixAPI methods via contextBridge (NET-02)');
  it.todo('does not expose ipcRenderer directly');
  it.todo('platform property matches process.platform');
});
