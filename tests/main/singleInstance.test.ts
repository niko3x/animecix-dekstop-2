import { describe, it } from 'vitest';

describe('Single Instance Lock', () => {
  it.todo('calls requestSingleInstanceLock (SHELL-02)');
  it.todo('quits if lock not obtained');
  it.todo('focuses existing window on second-instance event');
});
